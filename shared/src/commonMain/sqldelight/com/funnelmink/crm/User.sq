CREATE TABLE SQLUser (
    id TEXT PRIMARY KEY,
    email TEXT UNIQUE NOT NULL,
    username TEXT
);

insertUser:
INSERT INTO SQLUser(id, email, username)
VALUES(?, ?, ?);

selectUserById:
SELECT *
FROM SQLUser
WHERE id = ?;

selectAllUsersInfo:
SELECT SQLUser.*
FROM SQLUser;

removeAllUsers:
DELETE FROM SQLUser;