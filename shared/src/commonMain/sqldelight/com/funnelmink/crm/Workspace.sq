CREATE TABLE SQLWorkspace(
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    roles TEXT NOT NULL,
    avatarURL TEXT
);

insertWorkspace:
INSERT INTO SQLWorkspace(id, name, roles, avatarURL)
VALUES(?, ?, ?, ?);

selectWorkspaceById:
SELECT *
FROM SQLWorkspace
WHERE id = ?;

selectAllWorkspacesInfo:
SELECT SQLWorkspace.*
FROM SQLWorkspace;

updateWorkspace:
UPDATE SQLWorkspace
SET name = ?,
    roles = ?,
    avatarURL = ?
WHERE id = ?;

removeWorkspace:
DELETE FROM SQLWorkspace
WHERE id = ?;

removeAllWorkspaces:
DELETE FROM SQLWorkspace;